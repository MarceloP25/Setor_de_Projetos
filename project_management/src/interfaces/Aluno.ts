export interface Aluno {
  id: string;           
  nome: string;
  email: string;

  // Inserir variaveis para coleta de dados bancarios

  /** Referência ao projeto em que o aluno participa */
  nomeProjeto: string;
}